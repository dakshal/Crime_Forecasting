load('crime_type_#1.mat');
load('type1.mat');

random = randn(length(output(:, 1)), 1)*360000;

for i=1:length(output(:, 1))
    x = output(i, 8)/358;
    y = mod(output(i, 8), 309);
    
    rand_x = random(i)/600;
    rand_y = mod(random(i), 600);
    
    x = x + rand_x;
    y = y + rand_y;
    
    x = 
    
end
